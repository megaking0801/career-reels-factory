# 開發日誌

> 記錄每次工作的診斷、改動、研究與決策，方便日後接手。最新在最上面。

---

## 2026-06-29 ～ 06-30：成品「不能看」→ 後製重建 + avatar 方案研究

### 一、背景與問題
實測既有成品（job `debb8c2ac647`，用 D-ID 做的 avatar）發現根本不能 demo。抽幀診斷出**三個致命傷**：

1. **D-ID 免費版浮水印鋪滿整片** —— 後製無法移除。唯一解：換無浮水印的 avatar 來源。
2. **方形 1920×1920 avatar 塞進 9:16** —— 縮成 1080×1080 置中，上下大片死白邊、人很小、版面空洞。
3. **字幕是 whisper 重聽出來的簡體＋錯字（求職→「球职」、Power Point→「powerpoint」）＋整段蓋在臉上** —— 明明手上就有正確繁體腳本，卻拿去重聽反而聽爛。

### 二、本機後製重建（已完成並驗證）
與 avatar 來源無關、本機免費就能做的部分，全部重做：

| 改動 | 檔案 | 內容 |
|---|---|---|
| 新增口播語音 provider | `app/providers/voice/edge.py`、`__init__.py` | edge-tts 合成台灣中文 mp3（學長 `zh-TW-YunJheNeural` / 學姐 `zh-TW-HsiaoChenNeural`），回傳逐句時間軸（SentenceBoundary）。免費、免金鑰、要連網。 |
| 字幕來源改寫 | `app/media/captions.py` | 主路徑：用 edge-tts 時間軸直接產**正確繁體 SRT**（不經 whisper）。新增 SRT→ASS（寫死 `PlayResX/Y=1080/1920`）。whisper 改為**後備**：只取語音時間範圍、文字一律套已知腳本。 |
| 合成版面重做 | `app/media/compose.py` | 三區版面：上方藏鏡人提問字卡／中段 avatar／下三分之一回答字幕。背景用 avatar 模糊放大鋪滿＋壓暗 scrim（消死邊）。修 Windows 路徑跳脫（反斜線→正斜線）。新增 `probe_duration`。 |
| 流程串接 | `app/pipeline.py` | 腳本階段順產 mp3＋SRT＋走動場景 prompt；媒體階段優先用預產 SRT，時間對不上才用 whisper 後備（`_srt_matches`）。 |
| 端點/UI/設定 | `app/main.py`、`static/app.js`、`static/style.css`、`config.json`、`app/providers/prompts.py` | 新增 `/api/jobs/{id}/voiceover` 下載端點；卡片新增「下載口播 mp3」「複製走動場景 prompt」與操作指引；字幕加大到 60px、移到下三分之一；口播加長到約 45–60 秒。 |

**驗證結果（全綠）**：
- edge-tts → 正確繁體 SRT、斷行與時間合理。
- 拿既有 D-ID 素材跑新 compose：字幕回到下三分之一、不蓋臉、繁體；上方有提問字卡；死白邊消失（浮水印仍在，需換源才會消）。
- pipeline 直跑：腳本＋mp3＋SRT＋scene_prompt 都產出。
- server 煙霧測試：建 job→`script_ready`、`/voiceover` 回 200 audio/mpeg、欄位齊。

**踩雷筆記**：
- ffmpeg bin 在使用者 PATH，但**舊 shell 工作階段吃不到**，要新開 PowerShell；`run_windows.ps1` 沒有 `--reload`，**改程式碼要重啟 Python 行程**才生效（StaticFiles 例外，前端每次讀磁碟）。
- libass 用 SRT+force_style 時預設 384×288 虛擬畫布 → 字級/邊距被放大 6.7 倍（字爆大、跑到頂端）。解法：產 ASS 寫死 PlayRes，用真實像素。
- zh-TW edge-tts 回的是 SentenceBoundary（整句），不是逐字，需自行依字數切行。

### 三、avatar 來源研究（全網 + GitHub 掃過）
核心結論：**「全身走動＋場景＋對嘴」一定要 14B 級影片擴散模型**，輕量的只能做半身或頭。

- **全身會走＋對嘴（14B、12GB 要量化、慢）**：Wan2.2-S2V / Wan2.2-Animate（有現成 ComfyUI 工作流，圖＋走路參考片＋音檔→走動對嘴，**最現成**）、HuMo（ByteDance）、OmniAvatar-14B、MultiTalk（多人對話/訪問、15 秒）、UniAnimate/Animate Anyone 2（pose 驅動走路、但要另疊 lipsync）。
- **半身＋手勢（輕、12GB 順、快）**：EchoMimicV3（1.3B，官方「12G is all you need」、中文 OK）。
- **只有頭（最輕快、不會身體）**：SadTalker / MuseTalk / Hallo / LivePortrait / Sonic。
- **社群整合包**（好裝，底層多是頭/半身）：Linly-Talker、ai-avatar-system、Duix-Avatar。
- **付費 hosted（最穩）**：OmniHuman 1.5（fal.ai $0.16/秒≈NT$155/30 秒；A2E / JAI Portal 有免費額度可試）。

**A2E 實測踩雷**：「照片说话」只能讓靜態照片開口（不會走動）；「Image to Video」只 5 秒；**「照片说话」費用 6 金幣/秒**，免費 100 金幣只夠約 16 秒（已先做一支 12 秒 `test_short.mp3` 備用）。

### 四、關鍵決策
- avatar 採半/全自動的**可抽換 provider**（架構已預留 `app/providers/avatar/`）。
- 使用者要的是「**真的邊走邊講＋場景＋藏鏡人提問**」；提問用字卡疊（已內建），不需第二配音。
- **跨平台限制（重要）**：使用者公司電腦是 **macOS**，沒有 NVIDIA → 自架 14B 模型只能在家裡的 RTX 5070 跑，**搬不到 Mac**。
  - 「免費」綁 5070（只在家）；「跨平台（含 Mac）」就得走**雲端 API**（Replicate/fal 跑開源，或 OmniHuman），三者不能兼得。
  - 工具本身（FastAPI+ffmpeg+edge-tts）跨平台沒問題，卡住的只有「生 avatar」那一棒。
- **尚未拍板**：avatar 最終走 (A) 開源模型上 Replicate/fal、(B) OmniHuman on fal、還是 (C) 家裡自架 Wan2.2-Animate + Mac 另接雲端。

### 五、環境現況（家裡 Windows）
Python 3.12.10、`.venv` 套件齊（含 edge_tts）、`.env` 有 GROQ key（走 Groq）、FFmpeg 8.1.2 full build（subtitles/drawtext 齊，bin 在使用者 PATH）、GPU **RTX 5070 12GB**、RAM 31GB、C 槽剩 ~260GB、驅動 CUDA 13。server 目前跑在背景 127.0.0.1:8000（新程式碼）。

### 六、下一步 / 待辦
1. 決定 avatar 雲端方案（A/B/C），把對應 provider 接進 `app/providers/avatar/`，讓整條鏈半自動、跨平台。
2. （可選）家裡 5070 自架 Wan2.2-Animate（ComfyUI）當免費大量生方案。
3. 用第一支無浮水印走動 avatar 跑完整流程，微調字幕大小/位置/場景 prompt。
4. 更新 README 與首頁「指南」分頁：把舊的「拿去 Kling」流程改成現行 OmniHuman/雲端流程。
5. 本批改動尚**未 git commit**（依使用者指示先只驗證）。

### 七、相關檔案
- 企劃定位：`企劃與策略.md`
- 腳本模板：`起手腳本與Prompt模板.md`
- 程式：`career-reels-factory/app/`（main / pipeline / jobs / providers/{script,voice} / media/{captions,compose}）、`static/`、`config.json`
